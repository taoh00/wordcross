# 项目端口与脚本配置规则

此文件由 SuperTeam 系统自动生成，定义了本项目的端口分配和脚本位置。

## 重要提醒

> **端口已固定分配，请勿自定义！**
> 
> 本项目的端口已由系统统一分配，请始终使用下方指定的端口号。
> 请不要自定义启停脚本位置，始终更新 `scripts/` 目录下的脚本。

## 项目信息

- **项目ID**: 2
- **项目名称**: 我爱填单词
- **端口块起始**: 10010
- **端口范围**: 10010 - 10019

## 端口分配（已固定）

| 用途 | 端口号 | 说明 |
|------|--------|------|
| 前端主端口 | `10010` | 前端应用服务端口 |
| 前端备用端口 | `10011` | 前端备用/热重载端口 |
| 后端主端口 | `10012` | 后端 API 服务端口 |
| 后端备用端口 | `10013` | 后端备用端口 |
| 数据库端口 | `10014` | 数据库服务端口 |
| 缓存端口 | `10015` | Redis/缓存服务端口 |

## 脚本位置（固定）

所有启停和编译脚本都位于 `scripts/` 目录下，**请不要修改脚本位置**：

| 脚本 | 路径 | 用途 |
|------|------|------|
| 编译脚本 | `scripts/build.sh` | 编译项目（一键部署时自动调用） |
| 启动脚本 | `scripts/start.sh` | 启动服务（一键部署/重启时自动调用） |
| 停止脚本 | `scripts/stop.sh` | 停止服务 |

### 脚本使用方法

```bash
# 编译项目
./scripts/build.sh

# 启动服务
./scripts/start.sh

# 停止服务
./scripts/stop.sh
```

## 开发环境配置

### 前端开发
```bash
# 静态 HTML 项目（使用固定端口）
python3 -m http.server 10010 --bind 0.0.0.0
```

### 后端开发
```bash
# Python/FastAPI（使用固定端口）
uvicorn main:app --host 0.0.0.0 --port 10012 --reload
```

## 发布到 SuperTeam

使用内部发布脚本将应用发布到 SuperTeam 平台：

```bash
# 发布应用（端口已在脚本中配置）
python3 /root/SuperTeam/superteam/apps/backend/scripts/internal_publish.py \
    --project-id 2 \
    --frontend-port 10010 \
    --backend-port 10012 \
    --version "1.0.0"
```

详细发布指南请参考：`@deploy/publish-app`
